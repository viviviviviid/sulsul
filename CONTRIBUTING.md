# 기여하기

버그 수정, 사이트 재현 사례, 한국어 표현 개선, 다른 운영체제 지원을 환영합니다.

개발 환경·파일별 역할·검증 절차는 [에이전트 가이드](AGENTS.md)에 정리되어 있습니다. AI 에이전트에게 작업을 맡길 때도 이 문서를 안내하세요. 내부 동작은 [구조 설명](docs/architecture.md), 패키지 생성·공개는 [배포자 가이드](docs/publishing.md)를 참고하세요.

1. 저장소를 포크하고 변경 브랜치를 만듭니다.
2. Node.js LTS에서 `npm ci` 후 수정합니다.
3. `npm run check`, `npm test`를 실행합니다. 본문 추출·탭 이동·UI 변경은 `npx playwright install chromium` 후 `npm run test:browser`도 실행합니다.
4. PR에 문제가 발생하는 페이지·동작, 수정 후 동작, 검증 결과를 적습니다. 실제 AI와 가짜 응답을 구분하세요.

프로덕션 의존성을 추가하기 전에 Node 내장 API로 가능한지 검토하세요. 실행할 코드·URL을 페이지에서 받아 실행하지 마세요. AI 출력은 HTML로 삽입하지 않고 검증한 텍스트 노드만 바꿉니다.

새 파일은 `scripts/files.mjs` 공개 허용 목록에 추가합니다. 설치 후 전체 폴더를 커밋하거나 통째로 압축하지 마세요. `npm run package:source`는 공개 소스만 포함합니다.

테스트는 실제 AI 계정을 호출하지 않습니다. CI에 키나 로그인 정보를 넣을 필요가 없습니다. 브라우저 테스트는 임시 프로필과 가짜 페이지·응답으로 실행합니다.

개인 사용·수정에는 스토어 업로드가 필요하지 않습니다. [소스 설치](docs/installation.md#저장소에서-직접-설치)를 따르세요. 소스 재배포에는 MIT 저작권 고지와 라이선스를 유지합니다.

manifest의 `key`는 확장 ID를 고정하는 **공개키**입니다. 원본을 대신하는 개인 사용 포크는 유지할 수 있습니다. 독립 배포나 동시 설치에는 표시명·공개키·호스트 이름뿐 아니라 Mac 설치·데이터 경로와 배포·지원 주소도 분리해야 합니다. 스토어용 연결 프로그램은 실제 스토어 항목의 공개키로 다시 빌드합니다. [배포자 가이드](docs/publishing.md).
